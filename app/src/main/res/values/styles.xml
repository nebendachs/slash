<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LoginRegisterScreenInteractionElement">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:layout_marginLeft">10dp</item>
    </style>

    <style name="LoginRegisterScreenErrorMessageTextView" parent="LoginRegisterScreenInteractionElement">
        <item name="android:textColor">#ff0000</item>
    </style>

    <style name="LoginRegisterScreenInputElement" parent="LoginRegisterScreenInteractionElement">
        <item name="android:backgroundTint">#ff8800</item>
    </style>

    <style name="slashLayout">
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
    </style>

    <style name="slashObject">
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <style name="slashHeader" parent="slashObject">
        <item name="android:textSize">25sp</item>
    </style>

    <style name="MemeGeneratorTemplatesImageViews">
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginStart">10dp</item>
        <item name="android:layout_width">100dp</item>
        <item name="android:layout_height">100dp</item>
        <item name="android:scaleType">fitXY</item>
    </style>

</resources>
